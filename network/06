dans la vidéo le mec parle du fichier resolv.conf

on va voir le fichier /etc/resolv.conf
bon par chance je l'ai ouvert avec vim et pas cat donc y avait le path écrit, c'était pas /etc/resolv.conf

donc j'ai ls -l pour vérifier et voir le vrai chemin

ls -l /etc/resolv.conf
lrwxr-xr-x  1 root  wheel  22 Nov 20  2016 /etc/resolv.conf -> ../var/run/resolv.conf
donc le path est :  /var/run/resolv.conf
